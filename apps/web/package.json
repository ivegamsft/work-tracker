{
  "name": "@e-clat/web",
  "version": "0.1.0",
  "private": true,
  "description": "E-CLAT frontend — employee self-service and manager dashboards",
  "scripts": {
    "dev": "echo 'Frontend framework not yet selected — run squad init to scaffold'",
    "build": "echo 'No build configured yet'"
  }
}
